{
  "name": "geojson-schema",
  "version": "0.0.0",
  "scripts": {
    "pretest": "make lint",
    "test": "make test",
    "start": "nodemon --exec 'make test || exit 1' --watch test"
  },
  "devDependencies": {
    "ajv": "^6.2.0",
    "eslint": "^4.18.2",
    "eslint-config-tschaub": "^9.0.0",
    "globby": "^8.0.1",
    "nodemon": "^1.17.1"
  },
  "eslintConfig": {
    "extends": "tschaub"
  }
}
